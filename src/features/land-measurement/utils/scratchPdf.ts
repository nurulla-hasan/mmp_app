import type { SavedPlotRecord } from '../types/map';

export const createPrintImage = async (svgRef: any): Promise<string> => {
  return '';
};

export const downloadScratchPdf = async (svgRef: any, selectedPlots: SavedPlotRecord[]) => {
  return;
};
